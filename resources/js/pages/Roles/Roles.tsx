import { AppTemplate } from "@/templates/AppTemplate/AppTemplate";

const Roles = () => {
  return (
    <AppTemplate>
      <div style={{ margin: "auto" }}>
        <h1>Roles</h1>
      </div>
    </AppTemplate>
  );
};

export default Roles;
